PORT=4000
JWT_SECRET=change-me-to-a-long-random-string

# Only used the very first time the database is created (seeding is
# idempotent and skips accounts that already exist).
SEED_ADMIN_PASSWORD=0000
SEED_USER_PASSWORD=1111
